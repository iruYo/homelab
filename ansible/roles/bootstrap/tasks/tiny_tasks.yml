---
- name: Set local DNS server
  ansible.builtin.copy:
    dest: /etc/resolv.conf
    owner: root
    group: root
    mode: "0644"
    content: |
      {% for dns in bootstrap_dns_servers %}
      nameserver {{ dns }}
      {% endfor %}
  notify: Reboot
