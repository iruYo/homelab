---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helm.yml

configMapGenerator:
  - name: aws-pod-identity-webhook-helm-values
    files:
      - base-values.yml=values.yml

configurations:
  - kustomizeconfig.yml
