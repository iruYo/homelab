[Unit]
Description=Precision Time Protocol (PTP) service (Client)
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
ExecStart=/usr/sbin/ptp4l -f /etc/ptp4l-client.conf -i {{ iface }}
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target