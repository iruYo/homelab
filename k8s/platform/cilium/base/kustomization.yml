---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helm.yml
  - ip-pool.yml
  - l2-policy.yml

configMapGenerator:
  - name: cilium-helm-values
    files:
      - base-values.yml=values.yml

configurations:
  - kustomizeconfig.yml
