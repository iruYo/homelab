ingress:
  enabled: true

global:
  nodeSelector:
    storage: "true"

longhornManager:
  nodeSelector:
    storage: "true"

longhornDriver:
  nodeSelector:
    storage: "true"

metrics:
  serviceMonitor:
    enabled: false

defaultSettings:
  systemManagedComponentsNodeSelector: "storage:true"